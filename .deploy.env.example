# Скопируй в .deploy.env (он в .gitignore) и заполни.
# Используется Makefile-таргетами `deploy`, `tls-init`, `deploy-shell`, `prod-logs`, `prod-ps`.
DEPLOY_HOST=62.181.53.102
DEPLOY_USER=root
DEPLOY_PASS=put-server-password-here
DEPLOY_PATH=/root/Lesha
